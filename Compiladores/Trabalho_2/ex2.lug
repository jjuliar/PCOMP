void main {
  let float x;
  let float y;
  let bool flag;
  x := 5.0;
  y := 1.0;
  flag := true;

    while (flag == true) do {
        printIO x;
        x := add(x, y);
        flag := false;
    };


  if (x > 5.0) {
    printIO x;
  };
}

def float add(float a, float b) {
  return (a + b);
}